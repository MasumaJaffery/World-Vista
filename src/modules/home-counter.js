// Countries Card Counter!
const itemcount = async (countElement, countriesElements) => {
    countElement.textContent = `(${countriesElements.length})`;
  };
  // eslint-disable-next-line no-undef
  module.exports = { itemcount };